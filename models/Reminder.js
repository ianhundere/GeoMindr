const db = require('./db');

class Reminders {
    constructor(id, reminder, is_public) {
        this.id = id, 
        this.reminder = reminder, 
        this.is_public = is_public;
    }

    // === ===  CREATE  === ===
    
    
    // === ===  RETRIEVE  === ===


    // === ===  UPDATE  === ===


    // === ===  DELETE  === ===




}

